import React from "react";

export const metadata = {
  title: "Name Screening - Relationship Analysis",
};

export default function Layout({ children }) {
  return <React.Fragment>{children}</React.Fragment>;
}
